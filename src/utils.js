export const setUserColor = (currentColor) => {
  return `hsl(${currentColor}, 86%, 60%)`;
};

export const setBorderColor = (currentBorderColor) => {
  return `hsl(${currentBorderColor}, 60%, 50%)`;
};
